# 설계 노트

왜 이렇게 만들었는지 적어 둔 문서입니다. 쓰는 법은 [README](README.md)에
있습니다.

여기 적힌 것은 대부분 **한 번 다른 방법으로 해 보고 걷어낸 기록**입니다.
결정보다 그 결정을 내리게 만든 사정이 더 오래 쓸모 있어서 남깁니다.

## 3구역 시간축

모든 주제가 시간축 하나를 공유하고, 주제마다 레인을 하나씩 가집니다.

```
        업무            생활
      ┌──────────────┬──────────────┐
      │ [학회 26일]  │              │   대기 — 하루 이상 남은 일.
      │ [논문  9일]  │ [월세 17일]  │        레인별 균등 간격, 순서만 전달
  24H ├ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤   ← 이 선을 넘는 순간이 곧 알림
  12h │              │ [건강검진]   │   활주로 — 24시간 이내.
  DUE ┝━━━━━━━━━━━━━━━━━━━━━━━━━━━━┥        실제 시간 눈금 위에 놓임
      │ [확률론]     │              │   지남 — 오래 밀릴수록 깊이 가라앉음
      └──────────────┴──────────────┘
```

**뼈대는 한 번만 그립니다.** 주제마다 축을 통째로 복제하면 공간이 주제 수에
비례해 늘고, 축이 다르면 높이의 의미도 달라져 "업무의 3시간 뒤"와 "생활의
1시간 뒤" 중 뭐가 급한지 비교할 수 없습니다. 리마인더가 답해야 할 가장 중요한
질문을 구조가 막는 셈입니다.

**구역마다 표현이 다릅니다.** 한 축에 6시간과 26일을 같은 방식으로 그리면
가시성이 무너집니다. 먼 일정은 순서만 알면 되고, 임박한 일정은 정밀한 시간이
필요합니다.

**쓸 일이 없는 구역은 자리를 비웁니다.** 오늘 마감이 없으면 활주로가 접히고,
밀린 일이 없으면 지남 구역이 사라집니다. 붐비면 반대로 늘어나고, 창이 그
크기를 따라갑니다. 위젯에서 스크롤은 전체를 한눈에 본다는 전제를 깨므로 최후의
수단입니다.

## 임박 효과

색만으로 급함을 표현하면 색각 이상 사용자에게 전달되지 않습니다. 남은 시간을
**0~1 긴급도 값 하나**로 계산하고 여러 채널이 함께 반응합니다.

| 채널 | 여유 | 긴급 |
| --- | --- | --- |
| 위치 | 대기 구역 위쪽 | 활주로 바닥, 마감선에 근접 |
| 색 | 저채도 청록 | 고채도 적색 |
| 형태 | 둥근 모서리 | 모서리 축소, 좌측 스트라이프 굵어짐 |
| 타이포 | 상대 표기 "9일 남음" | 절대 시각 "23:59까지" |
| 모션 | 정지 | 6초 주기 호흡 |

야간에는 채도와 모션이 자동으로 억제됩니다. 밤에 빨간 게 깜빡이면 위젯을 끄게
되니까요.

**주제에는 색을 쓰지 않습니다.** 카드가 이미 긴급도 색을 쓰고 있어서 주제까지
색을 가지면 "이 색이 급하다는 뜻인지 주제라는 뜻인지" 헷갈립니다. 주제는 레인
위치와 이름으로 구분됩니다.

## 대비와 투명도

배경화면이 어두운 편이라 더 투명해도 괜찮다면 `src/lib/theme.json`의
`surface.background` 알파값을 낮추세요.

```json
"surface": { "background": "rgba(18,19,27,0.72)" }
```

기본값 `0.72`는 **순백 배경화면 위에서도** 본문 대비 6.65:1, 보조 텍스트
4.62:1을 확보하도록 잡은 값입니다(WCAG AA 기준 4.5:1). 낮출수록 배경이 비쳐
보이지만 밝은 배경화면에서 글자가 묻히기 시작합니다. `0.55` 아래로는 권하지
않습니다.

투명 창에서는 CSS `backdrop-filter`가 아무것도 흐리지 못합니다 — 창 뒤
바탕화면은 페이지 바깥이라 흐릴 대상이 아닙니다. 글자 대비는 전적으로 이
스크림이 책임집니다.

**Windows acrylic도 써봤지만 뺐습니다.** Windows 11은 비활성 창의 아크릴을
단색으로 대체하는데, 이 위젯은 거의 항상 비활성이라 회색 폴백만 보게 됩니다.

**`color-scheme: dark`는 `:root`에 걸지 않습니다.** 거기 걸면 WebKit이 페이지
바탕을 불투명한 검정으로 칠하고, 투명 창에서는 그게 곧 창 전체가 검은 판이
된다는 뜻입니다. WebView2는 이러지 않아서 Windows에서만 만들 때는 드러날 수
없었던 종류의 버그입니다. 우리가 원한 것은 바탕색이 아니라 기본 컨트롤이
어둡게 그려지는 것뿐이고, 그건 상속되는 성질이라 앱 뿌리에 걸어도 얻어집니다.

## 레인 폭

주제가 늘면 레인이 좁아집니다. `maxWidth`(내용 폭 상한)와 `laneMin`(레인 하한)
사이에서 나눠 쓰므로, 주제가 4개를 넘어도 위젯이 계속 넓어지지는 않습니다.
좁아져 잘린 제목은 카드에 마우스를 올리면 펼쳐집니다.

펼침은 **옆으로만** 자랍니다. 세로로 자라면 카드는 아래를 붙잡고 위로 커지는데,
그만큼 구역 위쪽을 넘어 잘리고 마감선에 닿습니다. 그래서 남은 시간과 마감
시각을 한 줄에 둡니다 — 예전에는 줄을 나눠 세 줄이었고, 그때는 카드가 옆으로
갈 데가 없었기 때문입니다.

## 바탕화면에 세워 두기

목표는 두 플랫폼이 같습니다 — **모든 앱 창보다 아래, 단 바탕화면보다는 위.**
그리고 바탕화면을 보여 달라는 요청에도 위젯은 남아 있어야 합니다. 위젯을
보려고 바탕화면을 부르는 것이니까요.

목표가 같아도 **방법은 정반대**입니다.

| | Windows | macOS |
| --- | --- | --- |
| 자리 유지 | 80ms마다 z-order 되돌리기 | 창 레벨 −1 (되돌릴 일 없음) |
| 바탕화면 보기 | Progman z-order 추격 | `stationary` 창 성질 |
| 작업표시줄/Dock | `skipTaskbar` | `LSUIElement` |
| 되찾는 길 | 트레이 | 트레이 + 앱 다시 실행 |

**Windows에는 그 자리가 없습니다.** `Win`+`D` 순간 Progman(바탕화면)이 z-order
200에서 22로 솟아오릅니다. 우리 창은 25쯤에 있었으니 그 아래로 밀리고,
배경화면이 위젯을 덮습니다. 창이 사라진 게 아니라 가려진 것이라 `IsIconic`도
DWM cloaked도 반응하지 않습니다. `alwaysOnBottom`은 답이 아닙니다 — 그건
바탕화면보다도 아래로 보냅니다. 그래서 포커스가 없을 때마다 자리를 되돌려
놓습니다.

**macOS에는 그 자리가 이미 있습니다.** `alwaysOnBottom`이 창 레벨을 −1로
내리는데, 이건 모든 앱 창보다 아래이면서 배경화면보다는 위입니다. 아무도
밀어내지 않으므로 쫓아다닐 필요가 없습니다.

남는 문제는 '데스크탑 보기'인데, 이건 z-order가 아니라 창에 붙는 성질 하나로
풉니다 — `NSWindowCollectionBehavior.stationary`. 레벨을 진짜 데스크탑 레벨까지
내리는 방법도 있고 macOS 바탕화면 위젯들이 보통 그렇게 하지만, 그 자리는
Finder의 바탕화면 아이콘 창 **아래**라 클릭을 전부 빼앗깁니다. 저 앱들이
하나같이 '상호작용 모드' 토글을 두는 이유가 그것입니다.

### 걷어낸 것 — WorkerW 부착

`Win`+`D`를 견디는 고전 기법(창을 바탕화면의 자식으로 만들기)도 시도했지만
걷어냈습니다. 붙은 창은 포커스 활성화 체인 밖이라 WebView2가 마우스를 전혀 받지
못해 클릭·드래그·스크롤이 모두 죽습니다. Rainmeter가 같은 기법으로도 조작을
유지하는 건 순수 Win32 창이라 자식 창에서도 마우스 메시지를 직접 처리하기
때문이고, 웹뷰에는 그 경로가 없습니다.

### macOS 메뉴 막대는 아이콘을 숨깁니다

노치가 있는 맥북에서 항목이 많으면 트레이 아이콘이 그 뒤로 밀려 아예 보이지
않습니다. Windows 트레이처럼 넘친 것을 모아 주는 버튼도 없어서, 위젯을 되찾는
유일한 손잡이가 소리 없이 사라집니다.

단축키를 트레이 메뉴에 적어 두는 것도 생각했지만 그건 답이 아닙니다. 메뉴를 열
수 있어야 읽을 수 있는데, 문제는 메뉴를 못 여는 상황입니다. 대신 사람이 실제로
하는 행동을 받습니다 — 앱을 잃으면 다시 실행합니다. macOS는 이미 도는 앱을 두
번 띄우는 대신 `applicationShouldHandleReopen`을 부르고, 그걸 받아 창을
꺼냅니다. 가르칠 것이 없다는 점이 이 방법의 값입니다.

### Windows는 두 번 뜹니다 — 막아야 합니다

같은 행동이 Windows에서는 다르게 끝납니다. 바로가기를 누르면 OS가 아무것도
묻지 않고 프로세스를 하나 더 띄웁니다.

보기 흉한 정도가 아니라 **데이터를 잃는 문제**입니다. 두 프로세스가 같은 파일에
할 일을 쓰는데 서로의 존재를 모릅니다. 한쪽이 읽은 뒤 다른 쪽이 쓰고, 먼저 읽은
쪽이 나중에 쓰면 그 사이에 적은 것이 사라집니다. 트레이 아이콘도 둘이 되고,
어느 쪽을 끄는 것인지 알 수 없습니다.

`tauri-plugin-single-instance`가 두 번째 실행을 막고 원래 프로세스에 알려 줍니다.
그 알림을 받아 창을 꺼냅니다 — macOS의 Reopen과 같은 동작이라, 두 플랫폼에서
'다시 실행'이 같은 뜻이 됩니다. 플러그인은 가장 먼저 걸어야 합니다.

## 캘린더는 왜 ICS인가

OAuth 대신 비공개 iCal 주소를 씁니다. Google Calendar 스코프는 민감 스코프로
분류돼 검증 전까지 평생 100명 상한이 걸리는 반면, 비공개 iCal 주소는 심사가
없고 Apple·Outlook 캘린더까지 같은 코드로 지원됩니다.

**대신 읽기 전용입니다.** `.ics`는 받아오는 파일이지 쓰는 동사가 없습니다.
그래서 구독한 일정은 고칠 수도 지울 수도 없고, 완료도 '옮기기'가 아니라 표시만
남기는 방식으로 처리합니다 — 옮겨 봐야 다음 폴링에 되살아납니다.

### 주소를 어디에 두는가 — 플랫폼마다 다릅니다

주소는 사실상 비밀번호입니다. 그래서 처음에는 양쪽 다 OS 자격 증명 저장소에
넣었습니다.

**macOS에서는 그 선택이 값을 요구했습니다.** 키체인은 항목마다 어떤 앱이 읽어도
되는지를 **코드 서명으로** 기억합니다. 우리는 ad-hoc 서명이라 빌드마다 서명이
달라지고, 업데이트한 앱은 macOS 눈에 남의 비밀을 달라는 낯선 프로그램입니다.
그래서 로그인 암호를 묻습니다. 클릭으로 넘길 수 있는 종류가 아니라 Developer ID
인증서로만 풀립니다.

**Windows 자격 증명 저장소에는 이 문제가 없습니다.** 앱을 서명으로 식별하지
않아서 다시 묻는 일이 없습니다.

그래서 갈랐습니다 — 값을 치르지 않는 쪽은 그대로 두고, 치르는 쪽만 앱 데이터
폴더의 파일로 내립니다. 플랫폼마다 다른 것이 어색해 보이지만 어색한 것은
상황이지 이 선택이 아닙니다. 같은 값을 치르지 않는데 같은 선택을 할 이유가
없습니다.

**잃는 것을 분명히 적어 둡니다.** macOS에서 같은 사용자로 도는 다른 앱이 그
파일을 물어보지 않고 읽을 수 있습니다. `~/Library/Application Support`는 TCC
보호 밖입니다. 파일 권한을 `0600`으로 두어 다른 사용자 계정은 막지만, 같은
계정의 프로그램은 막지 못합니다. 새는 것은 캘린더를 **읽는** 권한 하나이고
계정이나 쓰기 권한은 아니지만, 키체인이 막아 주던 것을 내려놓은 것은 맞습니다.

이 결정으로 **서명이 필요한 자리가 이 앱에서 사라졌습니다.** 남은 경고 둘은
브라우저로 받은 파일에만 붙는 표시 때문이라 첫 설치 한 번이면 끝납니다 — 앱이
스스로 받는 업데이트에는 그 표시가 붙지 않습니다.

예전 판에서 키체인에 넣어 둔 주소는 처음 읽을 때 한 번 파일로 옮겨 옵니다. 그
옮김이 마지막 키체인 접근입니다.

바뀌지 않는 것: 주소는 프런트로 돌려주지 않습니다 — 웹뷰에 두지 않는 것이
애초에 Rust에서 가져오기로 한 이유의 절반이었습니다. 그리고 한 번 꺼낸 주소는
프로세스가 사는 동안 들고 있습니다. 20분 주기로 저장소를 다시 여는 것은 전부
같은 답을 받으려고 여는 것이니까요.

일정은 '구간'이고 할 일은 '점'이라 성질이 다릅니다. `Task`에 끝 시각을 억지로
얹지 말고 별도 타입으로 나누는 편이 낫습니다.

## 수정과 삭제

**수정 폼을 따로 만들지 않았습니다.** 어려운 부분은 화면이 아니라 파싱이고 —
제목과 마감을 나누고, 주제를 고르고, 날짜·시각을 직접 지정하는 일 — 그건
입력칸이 이미 합니다. 폼을 하나 더 만들면 같은 말을 하는 방법이 둘이 되고,
파서가 바뀔 때마다 양쪽을 맞춰야 합니다. 위젯에서 가장 비싼 자원인 공간도
새로 듭니다.

**되돌려 넣은 제목은 다시 파싱하지 않습니다.** `내일 회의 준비`를 파싱하면
'내일'이 마감으로 끌려가고 제목에서 떨어져 나갑니다 — 멀쩡하던 제목이 조용히
잘리는 것입니다. 저장된 마감은 이미 사용자가 정한 값이므로 파싱 결과가 아니라
'사용자가 고친 값' 자리에 싣습니다.

**고치는 중인 카드는 완료되지 않습니다.** 테두리는 표시가 아니라 뜻입니다.
마감을 잘못 넣어 되돌리는 중에 항목이 기록으로 옮겨 가 버리면 고치던 값을 쓸
곳이 사라집니다. 상태를 나중에 수습하는 대신 만들지 않습니다.

**포커스를 잃는 것으로는 수정 모드에서 나가지지 않습니다.** 이 위젯은 포커스를
잃도록 설계된 물건이라, 다른 창을 한 번 볼 때마다 수정이 날아가면 안 됩니다.
다른 앱에서 무난한 관례가 여기서는 최악입니다.

**전역 단축키는 언제나 같은 상태를 보장합니다** — 빈 입력칸, 커서, 추가 준비.
수정 중이었다면 놓습니다. 그러지 않으면 사용자는 새 할 일을 적는다고 믿으면서
기존 항목을 덮어씁니다. 여기서 버리는 것은 확정하지 않은 초안이고, 반대쪽에서
잃는 것은 이미 저장된 값입니다.

**삭제에 확인을 두지 않습니다.** 완료가 한 번에 되는데 삭제만 두 번 눌러야 하면
다른 물건처럼 느껴집니다. 확인 창은 읽지 않고 누르는 습관을 만들지만 되돌리기는
실제로 되돌립니다. 지운 항목은 완료 기록에 넣지 않고 되돌리기 스택이 직접 들고
있습니다 — 기록은 '한 일'이고 지운 것은 하지 않은 일이라, 거기 넣으면 기록이
거짓말을 합니다.

**메뉴에 완료와 미루기는 넣지 않았습니다.** 완료는 카드에 이미 동그라미가
있어서 같은 일이 두 곳에 생깁니다. 미루기는 느낌이 불편할 때 사실을 바꾸는
버튼입니다 — 마감이 진짜 바뀌었다면 그건 수정이고, 안 바뀌었는데 옮기면 카드의
높이와 색이 거짓말을 시작합니다.

## 처음 켠 화면

예전에는 그럴듯한 가짜 할 일을 넣었습니다. 그런데 **이 위젯에서 카드의 자리는
시간에 대한 주장**입니다 — "발표 자료 준비 2일 뒤"는 읽는 사람의 일정에 대해
거짓을 말합니다. 사용자는 매번 "내가 이걸 적었나"를 판단해야 했습니다.

안내 카드는 자기가 서 있는 자리를 설명하므로 그 주장이 참입니다. 24시간 안쪽
카드는 정말로 24시간 안쪽에 있고, 기한 만료 카드는 정말로 지났습니다.

문구는 명사로 끝냅니다. 진짜 할 일 제목이 그렇게 생겼기 때문입니다. 그리고
'대기'나 '활주로' 같은 말은 쓰지 않습니다 — 그건 설계하며 우리가 붙인 이름이지
사용자가 화면 어디에서도 볼 수 없는 말입니다.

대부분의 문구는 **일부러 잘리게** 두었습니다. 잘린 제목은 손을 올리게 만들고,
올리면 카드가 펼쳐지는 것까지 같이 알게 됩니다. 구역을 가리키는 둘만
줄였습니다 — 앞부분만 남으면 무슨 구역인지가 사라지기 때문입니다.

## 기술 스택

Tauri 2 + Svelte 5 + TypeScript. Tauri는 OS가 이미 갖고 있는 웹뷰를 씁니다 —
Windows는 WebView2(Chromium), macOS는 WKWebView(WebKit). 그래서 배포 용량과
메모리가 Electron보다 크게 작고, 하루 종일 켜두는 위젯에서는 이 차이가
결정적입니다.

대신 렌더러가 플랫폼마다 다르다는 값을 치릅니다. 다만 이 위젯은 배치를
`getBoundingClientRect` 실측으로 계산하므로, 글꼴 메트릭이 달라져도 카드가
겹치거나 창 크기가 어긋나지 않습니다 — 3구역 시간축은 두 웹뷰에서 같은 코드로
섭니다.

## 코드 구조

| 파일 | 역할 |
| --- | --- |
| `src/lib/theme.json` | 색·치수·모션·글자 크기를 한 곳에 모은 테마 |
| `src/lib/urgency.ts` | 긴급도 단일 진실 공급원. 모든 시각 속성이 여기서 파생 |
| `src/lib/layout.ts` | 3구역 배치 계산. 구역 높이, 레인, 겹침 밀어내기 |
| `src/lib/parseInput.ts` | 한국어 자연어 입력 파서 |
| `src/lib/types.ts` | `Task`와 `TaskSource` 경계 |
| `src/lib/components/Column.svelte` | 시간축과 레인 렌더러 |
| `src/lib/components/QuickAdd.svelte` | 입력, 주제 고르기, 마감 날짜·시각. 수정 모드도 여기 |
| `src/lib/components/CardMenu.svelte` | 카드 우클릭 메뉴 — 수정·삭제 |
| `src-tauri/src/desktop/` | 위젯을 바탕화면 바로 위에 세워 두는 처리. 플랫폼마다 방법이 정반대라 파일이 갈립니다 |
| `src-tauri/src/ics.rs` | 캘린더 받아오기·펼치기, 주소 보관 |

`theme.json`은 사람이 읽고 고칠 수 있게 열어둔 파일입니다. 색 램프, 카드 반경,
글자 크기, 모션 강도, 레인 폭, 24시간 경계 시간을 바꿔 테마를 만들 수 있습니다.

## 배포

태그를 밀면 GitHub Actions가 Windows 인스톨러(`.exe`)와 macOS 디스크
이미지(`.dmg`)를 만들어 Releases에 **초안**으로 올립니다. 내용을 확인하고 직접
게시하는 흐름입니다.

```bash
npm version patch       # 커밋과 태그를 함께 만듭니다
git push --follow-tags  # 커밋만 밀면 워크플로가 돌지 않습니다
```

`tauri.conf.json`은 버전을 `../package.json`에서 읽으므로 따로 맞출 것이
없습니다. 두 곳에 같은 숫자를 적어 두면 언젠가 한쪽만 고치게 됩니다.

macOS 쪽은 `universal-apple-darwin`으로 한 번에 냅니다. Intel과 Apple Silicon을
따로 내면 받는 사람이 자기 Mac이 어느 쪽인지 알아야 하고, 잘못 받으면 실행이
안 되는 이유를 설명해야 합니다.

두 잡은 나란히가 아니라 **차례로** 돕니다. 둘 다 같은 릴리스의 `latest.json`을
고쳐 쓰는데, 동시에 쓰면 늦게 끝난 쪽이 먼저 끝난 쪽의 항목을 지울 수 있습니다.
그러면 한쪽 플랫폼 사용자 전원이 업데이트 경로를 잃습니다. **초안을 게시하기
전에 `latest.json`에 두 플랫폼이 다 들어 있는지 확인하세요.**

`main`에 밀 때마다 도는 `check` 워크플로가 두 플랫폼에서 컴파일을 확인합니다.
개발 환경이 Windows뿐이라 macOS 코드는 여기서 컴파일조차 할 수 없고, 이것이
없으면 오타 하나를 확인하는 데 릴리스 태그를 하나씩 태우게 됩니다.

| 시크릿 | 내용 |
| --- | --- |
| `TAURI_SIGNING_PRIVATE_KEY` | `gravitask.key` 파일 내용 전체 |
| `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` | 키 생성 시 정한 비밀번호 |

이 키는 코드 서명이 아니라 **업데이터 서명**입니다. 앱이 내려받은 파일이 우리가
올린 그 파일인지 확인하는 데만 쓰이고, OS의 Gatekeeper·SmartScreen과는 무관한
별개의 체계입니다.

**개인 키를 잃으면 업데이트 경로도 잃습니다.** 기존 사용자는 다른 키로 서명된
업데이트를 거부하므로 전원이 수동 재설치를 해야 합니다. 백업해 두세요.

릴리스 워크플로는 태그 푸시에서만 돕니다. 이 저장소는 공개라, `pull_request`로
트리거하면 포크에서 온 코드가 서명 키에 접근할 수 있습니다.

## 로드맵

- **v1** — 로컬 할 일만. 캘린더 연동 없음. 서명 없이 배포
- **v1.1** — 비공개 iCal(ICS) 주소로 캘린더 추가. `TaskSource` 구현체를 하나 더
  끼우는 형태라 UI 변경 없음. 항목 수정·삭제(우클릭 메뉴)도 함께
- **v1.2** — 코드 서명 인증서 *(선택)*

코드 서명은 이제 **있으면 좋은 것**이지 필요한 것이 아닙니다. 캘린더 주소를
키체인에서 내린 뒤로 서명을 요구하는 자리가 없어졌고, 남은 것은 첫 설치에서 한
번 뜨는 경고뿐입니다. 안내문을 줄 수 없는 사람에게 건네야 할 때 사는 것이지
그전에는 아닙니다.

애플 개발자 프로그램은 연 $99이고, Windows Authenticode는 그와 별개로 연
$200~400 수준입니다. Windows만 놓고 보면 Microsoft Store 개인 개발자 계정(1회
$19)이 훨씬 쌉니다 — 스토어가 대신 서명합니다.
